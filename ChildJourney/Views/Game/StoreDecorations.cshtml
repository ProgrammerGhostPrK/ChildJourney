@model ChildJourney.ViewModels.AdminViewModel
@using ChildJourney.Controllers;
@using Newtonsoft.Json
@{
    ViewData["Title"] = "Introduction";
    User response = JsonConvert.DeserializeObject<User>(Context.Session.GetString("CurrentUser"));
    foreach (var user in Model.Users)
    {
        if (user.Id == response.Id)
        {
            response = user;
        }
    }
}
<style>
    body {
        background-image: url("");
        background-repeat: no-repeat;
        background-attachment: fixed;
        background-size: cover;
    }
</style>

<div class="row">
    <div class="block">
        <h1>Store</h1>
        <label>@response.Coins Coins</label>
    </div>
</div>
<div class="row">
    <div class="block">
        @{
            if (Context.Session.GetString("HomeIsland") == "Active")
            {
                <a href="../Game/HomeIsland">Confirm</a>
            }
            else
            {
                <button><a asp-action="Islandpicking" asp-controller="Game">Confirm</a></button>
            }
        }
    </div>
</div>
<div class="row">
    <div class="block">
        <button><a asp-action="StoreBodyPart" asp-controller="Game">BodyParts</a></button>
    </div>
    <div class="block">
        <button><a asp-action="StoreClothing" asp-controller="Game">Clothing</a></button>
    </div>
    <div class="block">
        <button>Decorations</button>
    </div>
    <div class="block">
        <button><a asp-action="StoreIslands" asp-controller="Game">Islands</a></button>
    </div>
</div>


<div>
    <div class="block">
        <button id="1" onclick="menupicker(this.id)">Trees</button>
    </div>
    <div class="block">
        <button id="2" onclick="menupicker(this.id)">Roofs (House)</button>
    </div>
    <div class="block">
        <button id="3" onclick="menupicker(this.id)">Walls (House)</button>
    </div>
    <div class="block">
        <button id="4" onclick="menupicker(this.id)">Fences (House)</button>
    </div>
    <div class="block">
        <button id="5" onclick="menupicker(this.id)">Windows (House)</button>
    </div>
    <div class="block">
        <button id="6" onclick="menupicker(this.id)">Doors (House)</button>
    </div>
    <div class="block">
        <button id="7" onclick="menupicker(this.id)">Others</button>
    </div>
</div>


<div class="row">
    <div Id="pickwrapper1" class="container-fluid">
        <div class="row">
            <div class="inner" style="border-radius: 25px; border-color:lightgray">
                <label id="Dwindow">Trees</label>
            </div>
            <div style="height:500px">
                @foreach (var item in Model.decorations)
                {
                    var count = 0;
                    if (item.Type == "Tree")
                    {
                        <div style="display:inline-block ; width:130px ; margin:0 0 30px 0; background-color:white">
                            <button onclick="BuyDecoration('@item.Id')">
                                <table class="table">
                                    <tr>
                                        <td><img src="@item.Image" style="width:100px; height:100px;" align="center" /></td>
                                    </tr>
                                    <tr>
                                        <td><label align="center"> @item.Name @item.Type</label></td>
                                    </tr>
                                    <tr>
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId == response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label style="color:green">Owned</label>
                                            }
                                        }
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId != response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label>@item.Price Coins</label>
                                            }
                                        }
                                    </tr>
                                </table>
                            </button>
                        </div>
                    }
                }
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div Id="pickwrapper2" class="container-fluid">
        <div class="row">
            <div class="inner" style="border-radius: 25px; border-color:lightgray">
                <label id="Dwindow">Roofs (House)</label>
            </div>
            <div style="height:500px">
                @foreach (var item in Model.decorations)
                {
                    var count = 0;
                    if (item.Type == "Roof")
                    {
                        <div style="display:inline-block ; width:130px ; margin:0 0 30px 0; background-color:white">
                            <button onclick="BuyDecoration('@item.Id')">
                                <table class="table">
                                    <tr>
                                        <td><img src="@item.Image" style="width:100px; height:100px;" align="center" /></td>
                                    </tr>
                                    <tr>
                                        <td><label align="center"> @item.Name @item.Type</label></td>
                                    </tr>
                                    <tr>
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId == response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label style="color:green">Owned</label>
                                            }
                                        }
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId != response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label>@item.Price Coins</label>
                                            }
                                        }
                                    </tr>
                                </table>
                            </button>
                        </div>
                    }
                }
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div Id="pickwrapper3" class="container-fluid">
        <div class="row">
            <div class="inner" style="border-radius: 25px; border-color:lightgray">
                <label id="Dwindow">Walls (House)</label>
            </div>
            <div style="height:500px">
                @foreach (var item in Model.decorations)
                {
                    var count = 0;
                    if (item.Type == "Wall")
                    {
                        <div style="display:inline-block ; width:130px ; margin:0 0 30px 0; background-color:white">
                            <button onclick="BuyDecoration('@item.Id')">
                                <table class="table">
                                    <tr>
                                        <td><img src="@item.Image" style="width:100px; height:100px;" align="center" /></td>
                                    </tr>
                                    <tr>
                                        <td><label align="center"> @item.Name @item.Type</label></td>
                                    </tr>
                                    <tr>
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId == response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label style="color:green">Owned</label>
                                            }
                                        }
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId != response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label>@item.Price Coins</label>
                                            }
                                        }
                                    </tr>
                                </table>
                            </button>
                        </div>
                    }
                }
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div Id="pickwrapper4" class="container-fluid">
        <div class="row">
            <div class="inner" style="border-radius: 25px; border-color:lightgray">
                <label id="Dwindow">Fences (House)</label>
            </div>
            <div style="height:500px">
                @foreach (var item in Model.decorations)
                {
                    var count = 0;
                    if (item.Type == "Fence")
                    {
                        <div style="display:inline-block ; width:130px ; margin:0 0 30px 0; background-color:white">
                            <button onclick="BuyDecoration('@item.Id')">
                                <table class="table">
                                    <tr>
                                        <td><img src="@item.Image" style="width:100px; height:100px;" align="center" /></td>
                                    </tr>
                                    <tr>
                                        <td><label align="center"> @item.Name @item.Type</label></td>
                                    </tr>
                                    <tr>
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId == response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label style="color:green">Owned</label>
                                            }
                                        }
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId != response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label>@item.Price Coins</label>
                                            }
                                        }
                                    </tr>
                                </table>
                            </button>
                        </div>
                    }
                }
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div Id="pickwrapper5" class="container-fluid">
        <div class="row">
            <div class="inner" style="border-radius: 25px; border-color:lightgray">
                <label id="Dwindow">Windows (House)</label>
            </div>
            <div style="height:500px">
                @foreach (var item in Model.decorations)
                {
                    var count = 0;
                    if (item.Type == "Window")
                    {
                        <div style="display:inline-block ; width:130px ; margin:0 0 30px 0; background-color:white">
                            <button onclick="BuyDecoration('@item.Id')">
                                <table class="table">
                                    <tr>
                                        <td><img src="@item.Image" style="width:100px; height:100px;" align="center" /></td>
                                    </tr>
                                    <tr>
                                        <td><label align="center"> @item.Name @item.Type</label></td>
                                    </tr>
                                    <tr>
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId == response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label style="color:green">Owned</label>
                                            }
                                        }
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId != response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label>@item.Price Coins</label>
                                            }
                                        }
                                    </tr>
                                </table>
                            </button>
                        </div>
                    }
                }
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div Id="pickwrapper6" class="container-fluid">
        <div class="row">
            <div class="inner" style="border-radius: 25px; border-color:lightgray">
                <label id="Dwindow">Doors (House)</label>
            </div>
            <div style="height:500px">
                @foreach (var item in Model.decorations)
                {
                    var count = 0;
                    if (item.Type == "Door")
                    {
                        <div style="display:inline-block ; width:130px ; margin:0 0 30px 0; background-color:white">
                            <button onclick="BuyDecoration('@item.Id')">
                                <table class="table">
                                    <tr>
                                        <td><img src="@item.Image" style="width:100px; height:100px;" align="center" /></td>
                                    </tr>
                                    <tr>
                                        <td><label align="center"> @item.Name @item.Type</label></td>
                                    </tr>
                                    <tr>
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId == response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label style="color:green">Owned</label>
                                            }
                                        }
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId != response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label>@item.Price Coins</label>
                                            }
                                        }
                                    </tr>
                                </table>
                            </button>
                        </div>
                    }
                }
            </div>
        </div>
    </div>
</div>
<div class="row">
    <div Id="pickwrapper7" class="container-fluid">
        <div class="row">
            <div class="inner" style="border-radius: 25px; border-color:lightgray">
                <label id="Dwindow">Others</label>
            </div>
            <div style="height:500px">
                @foreach (var item in Model.decorations)
                {
                    var count = 0;
                    if (item.Type == "Other")
                    {
                        <div style="display:inline-block ; width:130px ; margin:0 0 30px 0; background-color:white">
                            <button onclick="BuyDecoration('@item.Id')">
                                <table class="table">
                                    <tr>
                                        <td><img src="@item.Image" style="width:100px; height:100px;" align="center" /></td>
                                    </tr>
                                    <tr>
                                        <td><label align="center"> @item.Name @item.Type</label></td>
                                    </tr>
                                    <tr>
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId == response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label style="color:green">Owned</label>
                                            }
                                        }
                                        @foreach (var Object in Model.UserDecoration)
                                        {
                                            if (Object.UserId != response.Id && Object.DecorationId == item.Id && count == 0)
                                            {
                                                count += 1;
                                                <label>@item.Price Coins</label>
                                            }
                                        }
                                    </tr>
                                </table>
                            </button>
                        </div>
                    }
                }
            </div>
        </div>
    </div>
</div>


<script>
    function menupicker(ButtonId) {
        document.getElementById("pickwrapper1").style.display = "none";
        document.getElementById("pickwrapper2").style.display = "none";
        document.getElementById("pickwrapper3").style.display = "none";
        document.getElementById("pickwrapper4").style.display = "none";
        document.getElementById("pickwrapper5").style.display = "none";
        document.getElementById("pickwrapper6").style.display = "none";
        document.getElementById("pickwrapper7").style.display = "none";
        document.getElementById("pickwrapper" + ButtonId).style.display = "block";
    }
</script>
