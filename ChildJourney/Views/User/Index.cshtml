@model IEnumerable<ChildJourney.Models.User>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coins)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Daystreak)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DailyStreak)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnlockedIslands)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Coins)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Daystreak)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DailyStreak)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UnlockedIslands)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
